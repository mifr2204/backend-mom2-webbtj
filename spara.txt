fetch anrop

let url = "http://blabla"

async function getData() {
    const response = await fetch(url);
    const data = await response.json();

    console.table(data);
}

Skicka

async function createworkplace(workplacename, title m.m.) {
    let workplace = {
        companyname: companyname,
        location: location,
        startdate: startdate,
        enddate: enddate,
        title: title,
        description: description
    };
    const response = await fetch(url, {
        method: "POST",
        headers: {
            "content-type": "application/json"
        },
        body: JSON.stringify(workplaces)
    });

    const data = await response.json();

    console.log(data);
}